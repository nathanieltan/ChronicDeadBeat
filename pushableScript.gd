extends KinematicBody2D

# class member variables go here, for example:
# var a = 2
# var b = "textvar"
"""var id = 1;
var parent;
var movevar = Vector2(0.0, 0.0)
var controller;
var targ;
func _ready():
	# Called every time the node is added to the scene.
	# Initialization here
	parent = get_parent()
	controller = parent.get_parent();
	pass
	
"""